name: Build All Apps (Flutter & Web APK)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-flutter-apk:
    name: Build Flutter Release APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Build Flutter Release APK
        run: |
          cd zapchat_flutter
          flutter pub get
          flutter build apk --release --no-tree-shake-icons

      - name: Upload Flutter APK
        uses: actions/upload-artifact@v4
        with:
          name: ZapChat-Flutter-Release-APK
          path: zapchat_flutter/build/app/outputs/flutter-apk/app-release.apk

  build-web-capacitor-apk:
    name: Build Web-Capacitor Release APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Set up Java JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Build Web App & Capacitor Release APK
        run: |
          cd frontend
          npm install
          npm run build
          npx cap sync android
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease

      - name: Upload Capacitor Web APK
        uses: actions/upload-artifact@v4
        with:
          name: ZapChat-Web-Capacitor-Release-APK
          path: frontend/android/app/build/outputs/apk/release/app-release-unsigned.apk
